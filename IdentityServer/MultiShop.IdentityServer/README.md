# Kurulum (.NET8 için)
```bash
dotnet new install Duende.IdentityServer.Templates
```

```bash
cd {projectPath}
```


- MultiShop.IdentityServer adýyla projemi ilgili dizine kurdum.
```bash
dotnet new isaspid --name MultiShop.IdentityServer
```

Þablon þu eylemi çalýþtýracak þekilde yapýlandýrýldý:
 Güncel komut: dotnet run /seed

Bu eylemi çalýþtýrmak istiyor musunuz [Y(evet)|N(hayýr)]?
- N